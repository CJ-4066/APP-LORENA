sonar.projectKey=lo-renaciente
sonar.organization=marcpd28-png
sonar.projectName=Lo Renaciente
sonar.projectVersion=1.0

# Rutas principales
sonar.sources=apps/api/src,apps/admin/src,apps/mobile/lib

# Incluir SOLO código fuente real para no gastar el límite de líneas
sonar.inclusions=**/*.ts,**/*.tsx,**/*.dart,**/*.js,**/*.jsx

# Excluir específicamente generados y pesados (bajando el total por debajo de 100k)
sonar.exclusions=**/node_modules/**,**/dist/**,**/build/**,**/.dart_tool/**,**/*.g.dart,**/*.freezed.dart,**/*.spec.ts,**/*.test.ts,**/generated_plugin_registrant.dart,**/app_i18n_literals.dart,**/mock-store.ts,**/badge-store.ts,**/persistent-store.ts

sonar.sourceEncoding=UTF-8
